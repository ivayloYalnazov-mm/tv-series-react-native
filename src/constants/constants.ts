const DEFAULT_INFINITE_SCROLL_STEP = 10;

const GENERIC_ERROR_MESSAGE =
  "We've run into an unexpected issue. Please try again lager.";

const BASE_API_URL = "https://api.tvmaze.com";

export { DEFAULT_INFINITE_SCROLL_STEP, BASE_API_URL, GENERIC_ERROR_MESSAGE };
