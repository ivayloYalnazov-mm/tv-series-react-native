import { StyleSheet } from "react-native";

export const pageLoadingIndicatorStyles = StyleSheet.create({
  loadingContainer: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },
});
